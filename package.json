{
  "name": "health",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "prettier --write packages/**/*.{ts,tsx,js,jsx}",
    "collect": "yarn workspace @health/data collect",
    "dev": "yarn workspace @health/tower dev",
    "export": "yarn workspace @health/tower export",
    "build": "yarn workspace @health/tower build"
  },
  "devDependencies": {
    "prettier": "^2.0.1",
    "ts-node": "^8.8.1",
    "typescript": "^3.8.3"
  }
}